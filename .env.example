FLICKR_ARCHIVE_PATH=./archive

# Set to false after the initial import to remove web import entirely (recommended
# on public servers once the archive is built). CLI import still works via SSH.
IMPORT_ENABLED=true

# Required on public deployments while IMPORT_ENABLED=true. When set, web import
# is hidden from the nav and requires this password at /import.
IMPORT_SECRET=
